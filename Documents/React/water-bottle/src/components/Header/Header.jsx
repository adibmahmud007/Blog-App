import PropTypes from 'prop-types';

const Header = () => {
   
    return (
        <div>
          
            
        </div>
    );
};
Header.propTypes={
    bottle: PropTypes.object.isRequired
}

export default Header;